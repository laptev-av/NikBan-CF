Функция ПолучитьАдресФайлаОтчетаВХранилище_СводныйОтчетПоСмене(КассоваяСменаСсылка, ТипФайла = Неопределено) экспорт
	ОтчетОбъект = Отчеты.nb_СводныйОтчетПоСмене_01.Создать();
	ОтчетОбъект.КассоваяСмена = КассоваяСменаСсылка;
	ТабДок = Новый ТабличныйДокумент;
	ОтчетОбъект.ЗаполнитьТабличныйДокументРезультатаОтчета(ТабДок);
	
	tmpФайл = ПолучитьИмяВременногоФайла();
	Если  ТипФайла = Неопределено Тогда
		ТипФайла = ТипФайлаТабличногоДокумента.HTML5;
	КонецЕсли;	
    ТабДок.Записать(tmpФайл,ТипФайла);
	ДвоичныеДанные = Новый ДвоичныеДанные(tmpФайл);
	
	АдресФайлаВХранилище = ПоместитьВоВременноеХранилище(ДвоичныеДанные);
	Возврат АдресФайлаВХранилище
КонецФункции	
