#Область ОбработчикиСобытийФормы

Процедура ПриЗаписи(Отказ)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	Если НЕ ЭтотОбъект.Значение И Константы.ИспользоватьЗаявкиНаРасходованиеДС.Получить() Тогда
		Константы.ИспользоватьЗаявкиНаРасходованиеДС.Установить(Ложь);
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти