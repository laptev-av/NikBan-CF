#Область ПрограммныйИнтерфейс

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Не Параметры.Свойство("МассивДублирующихПозиций") Тогда
		Отказ = Истина;
		Возврат;
	КонецЕсли;
	
	Список.Параметры.УстановитьЗначениеПараметра("МассивДублирующихПозиций",Параметры.МассивДублирующихПозиций);
	
КонецПроцедуры

#КонецОбласти