
#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытий

Процедура ПередЗаписью(Отказ)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	Если НЕ ЭтоГруппа Тогда
		Если НЕ ЗначениеЗаполнено(РегистрационныйНомерДекларации) Тогда
			Реквизиты = Справочники.НомераГТД.РегистрационныйНомерИСтранаВвоза(Код);
			РегистрационныйНомерДекларации = Реквизиты.РегистрационныйНомер;
			ПорядковыйНомерТовара = Реквизиты.ПорядковыйНомерТовара;
			СтранаВвозаНеРФ = Реквизиты.СтранаВвозаНеРФ;
		КонецЕсли;
		Если СтранаПроисхождения = Справочники.СтраныМира.Россия Тогда
			СтранаВвозаНеРФ = Истина;
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#КонецЕсли
